import { Component } from '@angular/core';

// moduleId:module.id and templateURL is used to load the contents from a separate html file.

@Component({
  moduleId:module.id,
  selector: 'my-app',
  templateUrl: 'app.component.html'
})
export class AppComponent { }
